{% extends 'base.html.twig' %}

{% block title %}Settings — Bybit Trader{% endblock %}

{% block scripts %}
<script src="/assets/js/settings.js"></script>
{% endblock %}

{% block content %}
<div class="settings">

    <div class="dashboard-header" style="margin-bottom:24px;">
        <h2><i class="bi bi-gear-fill"></i> Settings</h2>
    </div>

    {# ── Bybit API ────────────────────────────────────────────── #}
    <div class="settings-section">
        <h3><i class="bi bi-plug-fill"></i> Bybit API</h3>
        <form id="bybit-settings-form">
            <div class="form-group">
                <label>API Key:</label>
                <input type="text" id="bybit-api-key" name="api_key" placeholder="Enter API Key" autocomplete="off">
            </div>
            <div class="form-group">
                <label>API Secret:</label>
                <input type="password" id="bybit-api-secret" name="api_secret" placeholder="Enter API Secret" autocomplete="off">
            </div>
            <div class="form-group">
                <label>Base URL:</label>
                <input type="text" id="bybit-base-url" name="base_url" placeholder="https://api-testnet.bybit.com">
            </div>
            <div class="form-group">
                <input type="checkbox" id="bybit-testnet" name="testnet" checked>
                <span>Use test network (Testnet)</span>
            </div>
            <div class="buttons-row">
                <button type="submit" class="btn-primary">
                    <i class="bi bi-floppy-fill"></i> Save
                </button>
                <button type="button" id="bybit-test-btn" class="btn-secondary">
                    <i class="bi bi-wifi"></i> Test connection
                </button>
            </div>
        </form>
    </div>

    {# ── ChatGPT API ──────────────────────────────────────────── #}
    <div class="settings-section">
        <h3><i class="bi bi-stars"></i> ChatGPT API</h3>
        <form id="chatgpt-settings-form">
            <div class="form-group">
                <label>API Key:</label>
                <input type="text" id="chatgpt-api-key" name="api_key" placeholder="Enter OpenAI API Key" autocomplete="off">
            </div>
            <div class="form-group">
                <label>Model:</label>
                <select id="chatgpt-model" name="model">
                    <option value="gpt-4o">GPT-4o</option>
                    <option value="gpt-4">GPT-4</option>
                    <option value="gpt-3.5-turbo">GPT-3.5 Turbo</option>
                </select>
            </div>
            <div class="form-group">
                <label>Timeout (sec):</label>
                <input type="number" id="chatgpt-timeout" name="timeout" min="15" max="300" step="5" placeholder="60">
            </div>
            <div class="form-group">
                <input type="checkbox" id="chatgpt-enabled" name="enabled">
                <span>Use ChatGPT for analysis</span>
            </div>
            <div class="buttons-row">
                <button type="submit" class="btn-primary">
                    <i class="bi bi-floppy-fill"></i> Save
                </button>
                <button type="button" id="chatgpt-test-btn" class="btn-secondary">
                    <i class="bi bi-wifi"></i> Test connection
                </button>
            </div>
        </form>
    </div>

    {# ── DeepSeek API ─────────────────────────────────────────── #}
    <div class="settings-section">
        <h3><i class="bi bi-cpu-fill"></i> DeepSeek API (alternative LLM)</h3>
        <form id="deepseek-settings-form">
            <div class="form-group">
                <label>API Key:</label>
                <input type="text" id="deepseek-api-key" name="api_key" placeholder="Enter DeepSeek API Key" autocomplete="off">
            </div>
            <div class="form-group">
                <label>Model:</label>
                <select id="deepseek-model" name="model">
                    <option value="deepseek-chat">deepseek-chat</option>
                    <option value="deepseek-reasoner">deepseek-reasoner</option>
                </select>
            </div>
            <div class="form-group">
                <label>Timeout (sec):</label>
                <input type="number" id="deepseek-timeout" name="timeout" min="15" max="300" step="5" placeholder="120">
            </div>
            <div class="form-group">
                <input type="checkbox" id="deepseek-enabled" name="enabled">
                <span>Use DeepSeek when ChatGPT is unavailable</span>
            </div>
            <div class="buttons-row">
                <button type="submit" class="btn-primary">
                    <i class="bi bi-floppy-fill"></i> Save
                </button>
                <button type="button" id="deepseek-test-btn" class="btn-secondary">
                    <i class="bi bi-wifi"></i> Test connection
                </button>
            </div>
        </form>
    </div>

    {# ── Trading parameters ────────────────────────────────────── #}
    <div class="settings-section">
        <h3><i class="bi bi-sliders"></i> Trading Parameters</h3>
        <form id="trading-settings-form">
            <div class="form-group">
                <label>Margin mode (UTA):</label>
                <select id="trading-required-margin-mode" name="required_margin_mode">
                    <option value="auto">Auto (don't check)</option>
                    <option value="cross">Cross (REGULAR_MARGIN)</option>
                    <option value="isolated">Isolated (ISOLATED_MARGIN)</option>
                </select>
                <span class="hint">For UTA, mode is set at account level. Cross — shared margin; Isolated — per-position.</span>
            </div>
            <div class="form-group">
                <label>Max margin (USDT):</label>
                <input type="number" step="0.01" min="0" id="trading-max-position" name="max_position_usdt" placeholder="e.g. 100">
                <span class="hint">Max margin per position. Trade size (entry) = margin × leverage.</span>
            </div>
            <div class="form-group">
                <label>Min margin (USDT):</label>
                <input type="number" step="0.01" min="0" id="trading-min-position" name="min_position_usdt" placeholder="e.g. 10">
                <span class="hint">Don't open or partial close if margin is below this amount.</span>
            </div>
            <div class="form-group">
                <label>Min leverage:</label>
                <input type="number" min="1" id="trading-min-leverage" name="min_leverage" placeholder="e.g. 1">
            </div>
            <div class="form-group">
                <label>Max leverage:</label>
                <input type="number" min="1" id="trading-max-leverage" name="max_leverage" placeholder="e.g. 5">
            </div>
            <div class="form-group">
                <label>Aggressiveness:</label>
                <select id="trading-aggressiveness" name="aggressiveness">
                    <option value="conservative">Conservative</option>
                    <option value="balanced">Balanced</option>
                    <option value="aggressive">Aggressive</option>
                </select>
            </div>
            <div class="form-group">
                <label>Max positions managed by bot:</label>
                <input type="number" min="1" id="trading-max-managed" name="max_managed_positions" placeholder="e.g. 10">
            </div>
            <div class="form-group">
                <label>Min open positions for bot auto-open:</label>
                <input type="number" min="0" id="trading-auto-open-min" name="auto_open_min_positions" placeholder="e.g. 5">
            </div>
            <div class="form-group">
                <input type="checkbox" id="trading-auto-open-enabled" name="auto_open_enabled">
                <span>Allow bot to open trades when positions are below threshold</span>
            </div>
            <div class="form-group">
                <label>Bot timeframe (decision frequency):</label>
                <select id="trading-bot-timeframe" name="bot_timeframe">
                    <option value="1">1 min (scalping)</option>
                    <option value="5">5 min</option>
                    <option value="15">15 min</option>
                    <option value="30">30 min</option>
                    <option value="60">1 hour</option>
                    <option value="240">4 hours</option>
                    <option value="1440">1 day</option>
                </select>
                <span class="hint">Bot runs every minute but makes decisions only once per selected period.</span>
            </div>
            <div class="form-group">
                <label>History candles for analysis (max 60):</label>
                <input type="number" min="5" max="60" id="trading-history-candles" name="bot_history_candles" placeholder="60">
                <span class="hint">How many recent candles of the selected timeframe to pass to the bot per analysis.</span>
            </div>

            {# ── Rotational Grid Mode ──────────────────────────────────── #}
            <div class="form-group" style="margin-top:16px; padding-top:16px; border-top:1px solid rgba(255,255,255,0.1);">
                <h4 style="margin-bottom:8px;"><i class="bi bi-grid-3x3-gap-fill"></i> Rotational Grid</h4>
                <span class="hint">Position from fixed number of layers. Add at levels on pullback, unload on bounce.</span>
            </div>
            <div class="form-group">
                <label>Position mode:</label>
                <select id="trading-position-mode" name="position_mode">
                    <option value="single">Single (one position, classic)</option>
                    <option value="rotational_grid">Rotational Grid (layers + grid)</option>
                </select>
            </div>
            <div class="form-group" id="rotational-grid-options">
                <label>Max layers:</label>
                <input type="number" min="2" max="5" id="trading-max-layers" name="max_layers" placeholder="3">
                <span class="hint">Volume limit: max_layers × layer_size_usdt</span>
            </div>
            <div class="form-group" id="rotational-grid-layer-size">
                <label>Layer size (USDT):</label>
                <input type="number" step="1" min="10" id="trading-layer-size-usdt" name="layer_size_usdt" placeholder="50">
            </div>
            <div class="form-group" id="rotational-grid-step">
                <label>Grid step (%):</label>
                <input type="number" step="0.5" min="1" max="20" id="trading-grid-step-pct" name="grid_step_pct" placeholder="5">
                <span class="hint">Min distance between levels (3–5% typical)</span>
            </div>
            <div class="form-group" id="rotational-grid-reentry">
                <input type="checkbox" id="trading-grid-reentry" name="grid_reentry_enabled" checked>
                <span>Allow re-entry after unload</span>
            </div>
            <div class="form-group" id="rotational-grid-unload">
                <input type="checkbox" id="trading-unload-on-reclaim" name="unload_on_reclaim_level" checked>
                <span>Close 1 layer when price reclaims level above</span>
            </div>
            <div class="form-group" id="rotational-grid-base-layer">
                <input type="checkbox" id="trading-base-layer-persistent" name="base_layer_persistent" checked>
                <span>Keep base layer intact on unload</span>
            </div>
            <div class="form-group" id="rotational-grid-rot-chop">
                <input type="checkbox" id="trading-rotation-in-chop" name="rotation_allowed_in_chop" checked>
                <span>Allow rotation in chop (sideways)</span>
            </div>
            <div class="form-group" id="rotational-grid-rot-trend">
                <input type="checkbox" id="trading-rotation-in-trend" name="rotation_allowed_in_trend">
                <span>Allow rotation in trend</span>
            </div>

            <div class="buttons-row">
                <button type="submit" class="btn-primary">
                    <i class="bi bi-floppy-fill"></i> Save
                </button>
            </div>
        </form>
    </div>

    {# ── Risk Guards ───────────────────────────────────────────── #}
    <div class="settings-section" style="border-color: rgba(239,68,68,0.4);">
        <h3 style="color: var(--negative);"><i class="bi bi-shield-exclamation" style="color:var(--negative)"></i> Loss Protection (Risk Guards)</h3>
        <form id="risk-settings-form">
            <div class="form-group">
                <input type="checkbox" id="risk-trading-enabled" name="trading_enabled" checked>
                <span><strong>Trading enabled</strong> — uncheck to block ALL trading operations.</span>
            </div>
            <div class="form-group">
                <label>Daily loss limit (USDT):</label>
                <input type="number" step="0.01" min="0" id="risk-daily-loss" name="daily_loss_limit_usdt" placeholder="0 = disabled">
                <span class="hint">If daily PnL drops below −X USDT, the bot will automatically stop.</span>
            </div>
            <div class="form-group">
                <label>Max total risk (USDT margin):</label>
                <input type="number" step="0.01" min="0" id="risk-max-exposure" name="max_total_exposure_usdt" placeholder="0 = disabled">
                <span class="hint">Sum of all margin collateral (notional/leverage). If exceeded — no new positions.</span>
            </div>
            <div class="form-group">
                <label>Cooldown between actions per symbol (min):</label>
                <input type="number" min="0" id="risk-cooldown" name="action_cooldown_minutes" placeholder="30">
                <span class="hint">Min interval between trading actions per coin. 0 = no limit.</span>
            </div>
            <div class="form-group">
                <input type="checkbox" id="risk-strict-mode" name="bot_strict_mode">
                <span><strong>Strict mode:</strong> CLOSE_FULL and AVERAGE_IN require manual confirmation.</span>
            </div>
            <div class="form-group">
                <input type="checkbox" id="risk-canary-mode" name="canary_mode">
                <span><strong>Canary mode:</strong> ALL bot actions require confirmation. Enable when changing prompt/strategy.</span>
            </div>
            <div class="form-group">
                <label>Min. edge multiplier over costs (fees+slip+funding):</label>
                <input type="number" step="0.1" min="0" id="risk-min-edge-mult" name="min_edge_multiplier" placeholder="2">
                <span class="hint">Action blocked if edge &lt; costs × this multiplier. 0 = disabled.</span>
            </div>
            <div class="buttons-row">
                <button type="submit" class="btn-danger">
                    <i class="bi bi-floppy-fill"></i> Save
                </button>
            </div>
        </form>
    </div>

    {# ── Circuit Breaker ───────────────────────────────────────── #}
    <div class="settings-section" id="cb-section" style="border-color: rgba(245,158,11,0.4);">
        <h3><i class="bi bi-lightning-charge-fill" style="color:var(--warning)"></i> Circuit Breaker (auto-pause on failures)</h3>
        <p class="section-desc">Automatically pauses trading on a series of Bybit or LLM errors. Resumes after <em>cooldown</em> or manually.</p>

        <div id="cb-status-banner" style="display:none; margin-bottom:12px; padding:10px 14px; border-radius:8px; font-size:0.9rem;"></div>

        <form id="cb-settings-form">
            <div class="form-group">
                <input type="checkbox" id="cb-enabled" name="cb_enabled" checked>
                <span><strong>Circuit Breaker enabled</strong></span>
            </div>
            <div class="form-group">
                <label>Bybit API error threshold (consecutive):</label>
                <input type="number" min="1" max="50" id="cb-bybit-threshold" name="cb_bybit_threshold" value="5">
                <span class="hint">After N consecutive Bybit errors — trading is paused.</span>
            </div>
            <div class="form-group">
                <label>LLM failure threshold (consecutive):</label>
                <input type="number" min="1" max="50" id="cb-llm-threshold" name="cb_llm_threshold" value="3">
            </div>
            <div class="form-group">
                <label>LLM invalid response threshold (consecutive):</label>
                <input type="number" min="1" max="50" id="cb-llm-invalid-threshold" name="cb_llm_invalid_threshold" value="5">
            </div>
            <div class="form-group">
                <label>Cooldown after trigger (min):</label>
                <input type="number" min="1" max="1440" id="cb-cooldown" name="cb_cooldown_minutes" value="30">
                <span class="hint">After pause the bot will automatically resume.</span>
            </div>
            <div class="buttons-row">
                <button type="submit" class="btn-primary">
                    <i class="bi bi-floppy-fill"></i> Save
                </button>
                <button type="button" id="cb-status-btn" class="btn-secondary">
                    <i class="bi bi-activity"></i> Status
                </button>
                <button type="button" id="cb-reset-btn" class="btn-secondary" style="color:var(--warning)">
                    <i class="bi bi-arrow-counterclockwise"></i> Reset all
                </button>
            </div>
        </form>
    </div>

    {# ── Strategy Signals ─────────────────────────────────────── #}
    <div class="settings-section" style="border-color: rgba(99,102,241,0.3);">
        <h3><i class="bi bi-graph-up-arrow" style="color:#6366f1"></i> Strategy Signals</h3>
        <p class="section-desc">Индикаторы и профили для LLM. Параметры влияют на strategy blocks в промпте.</p>
        <form id="strategies-settings-form">
            <div class="form-group">
                <input type="checkbox" id="strategies-enabled" name="enabled" checked>
                <span><strong>Включить StrategySignals</strong></span>
            </div>
            <div class="form-group">
                <label>Профили по таймфрейму (мин → профиль):</label>
                <div style="display:grid; grid-template-columns: repeat(3, 1fr); gap:8px; max-width:400px;">
                    <div><label class="small">1m</label><input type="text" id="strat-prof-1" placeholder="scalp"></div>
                    <div><label class="small">5m</label><input type="text" id="strat-prof-5" placeholder="scalp"></div>
                    <div><label class="small">15m</label><input type="text" id="strat-prof-15" placeholder="intraday"></div>
                    <div><label class="small">60m</label><input type="text" id="strat-prof-60" placeholder="intraday"></div>
                    <div><label class="small">240m</label><input type="text" id="strat-prof-240" placeholder="swing"></div>
                    <div><label class="small">1440m</label><input type="text" id="strat-prof-1440" placeholder="swing"></div>
                </div>
            </div>
            <div class="form-group">
                <label>Индикаторы:</label>
                <div style="display:flex; flex-wrap:wrap; gap:16px;">
                    <label><input type="checkbox" id="strat-ema-enabled" checked> EMA (20/50)</label>
                    <label><input type="checkbox" id="strat-rsi-enabled" checked> RSI14 (70/30)</label>
                    <label><input type="checkbox" id="strat-atr-enabled" checked> ATR14</label>
                    <label><input type="checkbox" id="strat-chop-enabled" checked> Chop (порог <input type="number" step="0.05" id="strat-chop-threshold" value="0.65" style="width:50px">)</label>
                </div>
            </div>
            <div class="form-group">
                <label>Правила:</label>
                <div>
                    <label><input type="checkbox" id="strat-allow-avg" checked> Разрешать AVERAGE_IN</label><br>
                    <label><input type="checkbox" id="strat-block-avg-chop" checked> Блокировать AVERAGE_IN в chop</label><br>
                    <label><input type="checkbox" id="strat-prefer-trend" checked> Предпочитать MOVE_SL_TO_BE в тренде</label>
                </div>
            </div>
            <div class="form-group" style="border-top:1px solid var(--border); padding-top:12px; margin-top:8px;">
                <label><i class="bi bi-cpu"></i> Long-term Memory (retrieval layer)</label>
                <p class="hint" style="margin:4px 0 8px;">Бот может использовать прошлые сделки и инсайты как контекст для LLM. Отключено по умолчанию.</p>
                <div>
                    <label><input type="checkbox" id="strat-memory-enabled"> Включить retrieval перед LLM</label><br>
                    <label><input type="checkbox" id="strat-memory-write"> Записывать память при закрытии сделок</label><br>
                    <label style="margin-left:20px;">Top-K похожих кейсов: <input type="number" min="1" max="15" id="strat-memory-top-k" value="5" style="width:50px"></label><br>
                    <label style="margin-left:20px;">Lookback (дней): <input type="number" min="7" max="365" id="strat-memory-lookback" value="90" style="width:60px"></label>
                </div>
            </div>
            <div class="buttons-row">
                <button type="submit" class="btn-primary"><i class="bi bi-floppy-fill"></i> Save</button>
            </div>
        </form>
    </div>

    {# ── Alerts ────────────────────────────────────────────────── #}
    <div class="settings-section" style="border-color: rgba(16,185,129,0.3);">
        <h3><i class="bi bi-bell-fill" style="color:var(--positive)"></i> Алерты (Telegram / Webhook)</h3>
        <p class="section-desc">Получайте уведомления о сбоях LLM, ошибках Bybit, превышении лимитов риска.</p>
        <form id="alerts-settings-form">
            <div class="form-group">
                <label>Telegram Bot Token:</label>
                <input type="text" id="alerts-tg-token" name="telegram_bot_token" placeholder="123456:ABCdef...">
            </div>
            <div class="form-group">
                <label>Telegram Chat ID:</label>
                <input type="text" id="alerts-tg-chat" name="telegram_chat_id" placeholder="-1001234567890">
                <span class="hint">Узнать chat_id: добавьте бота в группу и проверьте /api/getUpdates</span>
            </div>
            <div class="form-group">
                <label>Webhook URL (Slack / Discord / Custom):</label>
                <input type="text" id="alerts-webhook" name="webhook_url" placeholder="https://hooks.slack.com/...">
            </div>
            <div class="form-group">
                <input type="checkbox" id="alerts-llm-failure" name="on_llm_failure" checked>
                <span>Уведомлять о сбоях LLM</span>
            </div>
            <div class="form-group">
                <input type="checkbox" id="alerts-invalid-resp" name="on_invalid_response" checked>
                <span>Уведомлять о невалидных ответах LLM</span>
            </div>
            <div class="form-group">
                <input type="checkbox" id="alerts-risk-limit" name="on_risk_limit" checked>
                <span>Уведомлять о превышении лимитов риска</span>
            </div>
            <div class="form-group">
                <input type="checkbox" id="alerts-bybit-error" name="on_bybit_error">
                <span>Уведомлять об ошибках Bybit API</span>
            </div>
            <div class="form-group">
                <input type="checkbox" id="alerts-repeated" name="on_repeated_failures" checked>
                <span>Уведомлять о повторяющихся ошибках</span>
            </div>
            <div class="form-group">
                <label>Порог повторяющихся ошибок (подряд):</label>
                <input type="number" min="1" id="alerts-threshold" name="repeated_failure_threshold" value="3">
            </div>
            <div class="form-group">
                <label>Cooldown алерта повторяющихся ошибок (мин):</label>
                <input type="number" min="1" id="alerts-repeated-cooldown" name="repeated_failure_cooldown_minutes" value="60" title="Не дублировать алерт по одному символу чаще чем раз в N минут">
            </div>
            <div class="buttons-row">
                <button type="submit" class="btn-primary">
                    <i class="bi bi-floppy-fill"></i> Save
                </button>
                <button type="button" id="test-alert-btn" class="btn-secondary">
                    <i class="bi bi-send"></i> Тестовый алерт
                </button>
            </div>
        </form>
    </div>

    <div id="settings-message" class="message"></div>
</div>
{% endblock %}
