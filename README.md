# Bybit Trader - Trading Bot с интеграцией ChatGPT

Приложение для автоматической торговли на Bybit с использованием ChatGPT для анализа рынка.

## Возможности

- 📊 **Dashboard** - отображение статистики, позиций и сделок в реальном времени
- ⚙️ **Настройки** - конфигурация API ключей для Bybit и ChatGPT
- 🤖 **Анализ рынка** - автоматический анализ с помощью ChatGPT
- 📈 **Статистика** - детальная статистика по сделкам (винрейт, профит-фактор и т.д.)
- 🔄 **Реальное время** - обновление данных каждые 30 секунд

## Требования

- PHP >= 8.1
- Composer
- Расширения PHP: json, curl

## Установка

1. Установите зависимости:
```bash
composer install
```

2. Создайте файл `.env` (если его нет):
```bash
APP_ENV=dev
APP_SECRET=your-secret-key-change-this-in-production-12345678901234567890
APP_DEBUG=1
```

3. Запустите встроенный сервер:
```bash
php -S localhost:8000 -t public
```

Или используйте любой другой веб-сервер (Apache, Nginx), настроив корневую директорию на `public/`.

## Использование

1. Откройте браузер и перейдите на `http://localhost:8000`
2. Перейдите в раздел "Настройки" и укажите:
   - **Bybit API Key** и **API Secret** (для тестовой сети используйте testnet ключи)
   - **ChatGPT API Key** (опционально, если хотите использовать реальный анализ)
3. Вернитесь на Dashboard для просмотра данных

## Настройки

### Bybit API

- **API Key** - ваш API ключ от Bybit
- **API Secret** - ваш секретный ключ
- **Testnet** - использовать тестовую сеть (рекомендуется для начала)
- **Base URL** - URL API (по умолчанию для testnet: `https://api-testnet.bybit.com`)

### ChatGPT API

- **API Key** - ваш OpenAI API ключ
- **Модель** - выберите модель (GPT-4 или GPT-3.5 Turbo)
- **Включено** - активировать использование ChatGPT для анализа

## Структура проекта

```
bybit_trader/
├── config/          # Конфигурация Symfony
├── public/          # Публичная директория (точка входа)
│   └── assets/      # CSS, JS, jQuery
├── src/
│   ├── Controller/  # Контроллеры
│   └── Service/     # Сервисы (Bybit, ChatGPT, Settings)
├── templates/       # Twig шаблоны
└── var/            # Настройки и кэш (создается автоматически)
```

## API Endpoints

- `GET /` - Dashboard
- `GET /settings` - Страница настроек
- `GET /api/positions` - Получить текущие позиции
- `GET /api/trades?limit=100` - Получить сделки
- `GET /api/statistics` - Получить статистику
- `GET /api/market-data/{symbol}` - Получить рыночные данные
- `GET /api/market-analysis/{symbol}` - Анализ рынка через ChatGPT
- `GET /api/trading-decision/{symbol}` - Получить торговое решение
- `GET /api/settings` - Получить настройки
- `POST /api/settings` - Сохранить настройки

## Примечания

- По умолчанию приложение работает с мокап-данными, если API ключи не настроены
- Настройки сохраняются в `var/settings.json`
- Приложение работает полностью офлайн (кроме запросов к Bybit и ChatGPT API)

## Разработка

Приложение создано на Symfony 6.4 с использованием:
- Twig для шаблонов
- jQuery для фронтенда
- Symfony HTTP Client для API запросов

## Лицензия

MIT
