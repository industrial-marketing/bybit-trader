feat: atomic file I/O, idempotent bot tick, brand UI overhaul

Reliability:
- AtomicFileStorage: flock + temp-rename for all JSON state files
- BotRunService: timeframe-bucket idempotency prevents concurrent ticks
- BotHistoryService, PositionLockService, PendingActionsService rewritten
- BybitService caches use AtomicFileStorage
- ApiController: BotRunService replaces last-tick timestamp check
- New endpoint GET /api/bot/runs

Brand and UI:
- CSS design system with 14 CSS custom properties
- Bootstrap Icons 1.11 via CDN, icons on nav, buttons, section headers
- Brand logo and icon in public/assets/brand/ with CSS fallback
- Buttons: primary orange, secondary navy, danger red, success green
- Side badges: side-long green, side-short red, lock-badge
- Bot status as structured bot-alert component
- settings.html.twig: icons per section, Risk Guards red border
- dashboard.html.twig: icons on h2, num-aligned columns, icon buttons
- app.js: side badges, icon lock/close buttons, structured bot-alert HTML
